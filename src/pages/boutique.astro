---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Contact from '../components/Contact.astro';
import Shop from '../components/Shop.astro';

// Configuration
const title = "Aujo | Boutique";
const description = "Boutique officielle d'Aujo. Achetez des albums, des merchandising et d'autres produits exclusifs.";
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const siteUrl = Astro.url.origin;
---

<!-- Chargement officiel de Snipcart -->
<script src="https://cdn.snipcart.com/themes/v3.3.1/default/snipcart.js" defer></script>
<link rel="stylesheet" href="https://cdn.snipcart.com/themes/v3.3.1/default/snipcart.css" />

<!-- Styles pour gérer l'ordre d'empilement -->
<style is:global>
  .snipcart-modal,
  .snipcart-checkout-page,
  .snipcart-cart,
  #snipcart {
    z-index: 999999 !important;
    position: relative;
  }

  header.fixed {
    z-index: 50 !important;
  }

  body.snipcart-checkout-visible,
  body.snipcart-sidecart--opened {
    overflow: hidden;
  }
</style>

<Layout title={title} description={description} canonicalURL={canonicalURL.href}>
  <Navigation />
  <main>
    <div class="container mx-auto px-4 py-12">
      <div class="flex justify-between items-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold">Boutique</h1>
        
        <!-- Bouton de panier standard -->
        <button class="snipcart-checkout flex items-center bg-indigo-600 hover:bg-indigo-700 text-white py-2 px-4 rounded-lg">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
          </svg>
          <span class="snipcart-items-count mr-2">0</span> articles
        </button>
      </div>
      <Shop siteUrl={siteUrl} />
    </div>
  </main>
  <Contact />
</Layout>

<!-- Conteneur Snipcart -->
<div hidden id="snipcart" data-api-key="MTYzYzM2ODMtZWFlYy00MGJiLThkMDAtM2QzN2YwMGVkYzFiNjM2OTQ0MzAxMDE5OTU2NjQ0"></div>

<!-- Script de débogage -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    console.log('DOM fully loaded');
    
    // Vérifier si Snipcart est chargé
    const checkSnipcart = () => {
      if (window.Snipcart) {
        console.log('Snipcart is loaded and ready');
        return true;
      }
      console.log('Snipcart not yet loaded');
      return false;
    };
    
    // Vérifier régulièrement si Snipcart est chargé
    const interval = setInterval(() => {
      if (checkSnipcart()) {
        clearInterval(interval);
      }
    }, 500);
    
    // Écouter les événements Snipcart
    document.addEventListener('snipcart.ready', () => {
      console.log('Snipcart ready event fired');
    });
    
    document.addEventListener('snipcart.error', (error) => {
      console.error('Snipcart error:', error);
    });
  });
</script>

<div class="container mx-auto mt-8 p-4 bg-gray-100 rounded-lg">
  <h2 class="text-2xl mb-4">Test Bouton</h2>
  <button
    class="snipcart-add-item bg-green-600 hover:bg-green-700 text-white py-2 px-4 rounded-lg"
    data-item-id="test-item"
    data-item-name="Article Test"
    data-item-price="9.99"
    data-item-url={siteUrl} 
    data-item-description="Ceci est un article de test"
  >
    TESTER SNIPCART
  </button>
</div> 